{
  "name": "@ai-sdlc/dogfood",
  "version": "0.1.0",
  "private": true,
  "description": "AI-SDLC dogfood pipeline — uses the framework to govern its own repo",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./runner": {
      "types": "./dist/runner/index.d.ts",
      "import": "./dist/runner/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "execute": "tsx src/cli.ts",
    "fix-ci": "tsx src/cli-fix-ci.ts",
    "triage": "tsx src/cli-triage.ts",
    "watch": "tsx src/cli-watch.ts",
    "admit": "tsx src/cli-admit.ts",
    "review": "tsx src/cli-review.ts",
    "ppa-score": "tsx scripts/ppa-score.ts"
  },
  "dependencies": {
    "@ai-sdlc/orchestrator": "workspace:*",
    "@ai-sdlc/reference": "workspace:*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
